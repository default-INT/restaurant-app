import { StyleSheet } from 'react-native';
import { colors } from '@root/constants/colors';

export const styles = StyleSheet.create({
  error: {
    fontSize: 12,
    marginTop: 5,
    marginLeft: 6,
    color: colors.dangerousRegular,
  },
});
